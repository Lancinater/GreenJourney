import Alert from 'react-bootstrap/Alert';

function Description() {
    return (
        <Alert variant="primary" style={{textAlign: 'center'}}>
          In this interface, you can view the carbon emissions of each state in Australia, which can help you understand the state of each state.
        </Alert>
      );
}

export default Description;